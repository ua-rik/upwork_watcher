# upwork_watcher
Personal assistant tool for filtering new Upwork job postings. The application fetches new jobs via the official API, applies basic filters (budget, payment verification, keywords), and then scores them against user skills. The results are delivered through Telegram/Email. 
